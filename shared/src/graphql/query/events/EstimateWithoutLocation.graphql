query GetEventEstimateWithoutLocation($id: Uuid!) {
    events {
        get(id: $id) {
            estimateWithoutLocation {
                stopEtas {
                    eta
                }
                queuePosition
            }
        }
    }
}


