query GetEventEstimate($id: Uuid!, $form: FormReservation!) {
    events {
        get(id: $id) {
            estimate(form: $form) {
                timeEstimate {
                    pickup
                    arrival
                }
                queuePosition
            }
        }
    }
}

